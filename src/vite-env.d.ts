/// <reference types="vite/client" />

// Injected by `define` in vite.demo.config.ts for the landing/demo build. The
// library build never references it; it exists so the demo App.vue type-checks.
declare const __PKG_VERSION__: string;
